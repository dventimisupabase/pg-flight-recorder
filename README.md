# pg-flight-recorder (redirect shim)

This repository exists only to redirect the legacy GitHub Pages URL
`https://dventimisupabase.github.io/pg-flight-recorder/` to the renamed
canonical site `https://dventimisupabase.github.io/pg_flight_recorder/`.

The project was renamed from `pg-flight-recorder` to `pg_flight_recorder`
for naming consistency with PostgreSQL identifiers (which cannot contain
unquoted hyphens).

The actual project lives at:
<https://github.com/dventimisupabase/pg_flight_recorder>
